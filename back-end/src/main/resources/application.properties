spring.application.name=back-end

# Load env tá»« file .env (docker-compose)
spring.config.import=optional:file:.env[.properties]

# ===============================
# DATASOURCE (MYSQL OUTSIDE DOCKER)
# ===============================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# ===============================
# HIKARI POOL (T?I ?U CHO VPS - wait_timeout=28800s)
# ===============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
# Idle 10 phút
spring.datasource.hikari.idle-timeout=600000
# ? 15 PHÚT ? AN TOÀN TUY?T ??I
spring.datasource.hikari.max-lifetime=900000
# Ping 5 phút / l?n
spring.datasource.hikari.keepalive-time=300000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
# Leak detection ch? b?t khi debug
spring.datasource.hikari.leak-detection-threshold=0


# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# ===============================
# LOGGING (PRODUCTION SAFE)
# ===============================
logging.level.com.zaxxer.hikari=WARN
logging.level.org.hibernate.SQL=WARN

# ===============================
# JWT
# ===============================
key-secret=${JWT_KEY_SECRET}
access-token-expiration=900000
refresh-token-expiration=604800000

# ===============================
# EMAIL
# ===============================
gmail.email=${EMAIL}
gmail.password=${PASSWORD_EMAIL}
